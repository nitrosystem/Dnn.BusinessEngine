using System;
using System.Collections.Generic;
using System.Linq;

namespace NitroSystem.Dnn.BusinessEngine.Shared.Helpers
{
    public static class CollectionHelper
    {
        //public static IEnumerable<TChild> GetChildsFromList<TParent, TChild>(
        //    IEnumerable<TParent> parents,
        //    IEnumerable<TChild> childs,
        //    Func<TChild, object> childKeySelector)
        //    where TParent : new()
        //    where TChild : new()
        //{
        //    var lookup = (childs ?? Enumerable.Empty<TChild>())
        //        .GroupBy(childKeySelector)
        //        .ToDictionary(g => g.Key,
        //                      g => g.AsEnumerable());
        //}
    }
}
